document.addEventListener('DOMContentLoaded', async () => {
	saveBtn = $('save_button');
	loadBtn = $('load_button')
	
    saveBtn.addEventListener('click', clickOnSave);
});

let saveBtn, loadBtn;

function clickOnSave() {
    
}